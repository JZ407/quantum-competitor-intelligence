# 开发规范符合性评审报告（2026-09-01）

评审基准：`D:\Claude_code\开发规范.md` v1.1。方法：3 个并行评审代理逐项对照，结论均带 file:line 证据。

## 一、合规总览

| 项目 | 红线违规 | 主要缺口 | 亮点 |
|---|---|---|---|
| ragflow_upload（新） | 0 | tests/、CLAUDE.md、README 验收标准、ruff 未装 | 结构/秘密/README/日志前缀全部达标，规范后参照系 |
| rag_system | 明文密码 2 处；retag_historical 产生旧格式 tags | CLAUDE.md、tests/、运行产物进 git | LLM key 已环境变量化；单一事实源达标 |
| liangke_daily | 明文密码 1 处；5 个日期脚本（规范点名反例） | CLAUDE.md、tests/、跨项目读 rag_system config | tags 三分字典达标；运行产物 gitignore 达标 |
| knowledge_graph | config.yaml:23 明文密码（已入库） | CLAUDE.md、tests/、.gitignore 缺 __pycache__ | 单一事实源（纯消费者）达标；processed_state 机制正常 |
| institution_news | 0（间接带入明文） | CLAUDE.md/README/tests 全无；3 个 backfill 脚本；硬编码绝对路径 | 自身代码无明文密钥 |
| competitor_profiles | **非 git 仓库**；8 个写入方+跨项目双写 profile_publications | 无 .gitignore、双版本脚本 _v2 | 无明文密钥 |
| conference_db | 0 | 非 git 仓库；README 无 | 唯一写入方达标；无日期脚本 |
| 仓库根 | **明文密码 5 处**；46 项未提交；5 个 gitlink 无 .gitmodules；备份 8 天未跑 | SOP/HANDOFF/ARCHITECTURE 过时（未含第 7 仓） | backup.ps1 覆盖面达标；根 .gitignore 良好 |

## 二、真 Bug（评审意外发现）

- **knowledge_graph/core/build_graph.py:77**：`elif isinstance(tg, list)` 引用未定义变量 `tg`（应为 `std_tags`），且外层 try/except 只捕 JSONDecodeError/TypeError——**遇到任何旧 flat-list 格式 tags 即整个增量构建 NameError 崩溃**。

## 三、立即整改清单（红线违规，按风险排序）

1. **明文 MySQL 密码 scraper123 共 10 处**，全部替换为 `${LIANGKE_MYSQL_PASSWORD}`：
   `rag_system/examples/sync_liangke.py:25`、`rag_system/examples/daily_report_app.py:37`、`liangke_daily/core/db.py:17`、`liangke_daily/core/sync_to_onedrive.py:20`、`knowledge_graph/config.yaml:23`（已 git 入库）、`services.json:8`、`startup.bat:9,24`、`backup.ps1:85`、`HANDOFF.md:48,151-152`、`ragflow_upload/README.md:58`
2. **修复 build_graph.py:77 未定义变量**，补一条"旧格式 tags"冒烟测试
3. **liangke_daily 5 个 websearch 脚本归并**为一个参数化脚本，日期脚本移入 archive/（规范点名反例）
4. **主仓库工作区清理**：提交 43 项 dataprojection 删除；5 个 gitlink 补 .gitmodules（当前 git submodule 命令 fatal，clone 无法复现拓扑）
5. **competitor_profiles 补 git init + .gitignore**；收敛 profile_publications 双写入方（institution_news/scrape_psiquantum_papers.py:147-160 与 extract_xanadu_authors.py:130-136 的 DELETE+全量 INSERT 互相覆盖）
6. **恢复备份纪律**：backup.ps1 注册 Windows 计划任务（每日）；zip 自动拷 OneDrive；补季度恢复演练记录
7. **rag_system/examples/retag_historical.py:59 停产生 flat list**，改为三分字典

## 四、短期整改清单

1. 全仓安装 ruff，跑通 `ruff check` 基线（当前 `No module named ruff`）
2. 每个项目补 CLAUDE.md + 关键管线 tests/ 冒烟（ragflow_upload 的 dry-run 冒烟最先）
3. git 清除已跟踪运行产物：rag_system 的 `index/`、`data/*.pdf`、`weekly_temp/`、`examples/investments.json`；institution_news 的 `data/quera/*.json`；主仓的 `competitor_profiles/profiles.db`（git rm --cached）
4. 跨项目硬编码绝对路径收敛：institution_news core/llm.py:5 等 3 处、knowledge_graph core/llm.py:4、liangke_daily 读 rag_system config.yaml 的 3 处
5. Cookie 单一事实源：以 pickle 为唯一源，cookies.txt 只作导入入口，消除三处并存漂移
6. 三份工作区文档（SOP/HANDOFF/ARCHITECTURE）对齐现状：登记第 7 个仓库 ragflow_upload、RAGFlow 上传与 backup 任务写入 SOP 周期表、修正"6 个仓库/4 repos"过时表述
7. startup.bat 与 services.json 双轨合并（或标注 deprecated）
8. 合并 QuEra/update_quera_team.py 与 _v2.py 双版本

## 五、长期清单（2026-10 期限）

1. `D:\Claude_code\lib\` 共享库：Config 类/DB 适配器/LLM 客户端三副本抽取（规范 2.5 存量过渡条款）
2. 项目结构收敛（pyproject.toml、liangke_daily 项目内 config.yaml）
3. knowledge_graph processed_state 中途落盘（防整轮崩溃丢游标）
4. lit-review/archive 补 README；conference_db build_db.py 参数化（--json-path/--year）

## 六、已达标的项（保持即可）

- ragflow_upload：结构模板、秘密处理、README"给人的质量"、日志前缀、UTF-8 运行约定
- liangke_daily：tags 三分字典构造、运行产物 .gitignore、cookie 检查脚本
- rag_system：LLM key 环境变量化（2026-07-19 已清）、单一事实源只读纪律
- knowledge_graph：纯消费者定位、增量游标持久化
- conference_db：唯一写入方、重建式幂等
- backup.ps1 覆盖面（MySQL dump + 全部 .db + 记忆库 + RAG 语料）
- 各仓 2026-07 之后的提交信息基本已规范
