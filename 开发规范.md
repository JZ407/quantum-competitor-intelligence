# 开发规范（2026-09 起生效）

> 本规范为「一个人 + Claude Code」的规模定制：只保留真正防屎山、提效率的规则，
> 不引入任何重流程。参考：敏捷宣言（小团队小项目的小纪律）、PEP 8、
> 规范驱动开发 SDD（先写规格再写代码）。
>
> **一句话总纲：先想清楚要什么（规格），再动手；数据只有一个事实源；重复出现第三次的东西必须抽成共享。**

---

## 一、流程层：每个功能先立一页纸规格

### 1.1 规格（Spec）模板

任何**超过半天、或预计改动 ≥ 3 个文件**的工作，动工前先写一页纸规格（写在计划文件或项目 README 顶部）：

```
目的：解决什么问题 / 谁在用 / 为什么现在做
范围：做什么（明确列出）、不做什么（明确排除）
验收标准：做完后怎么证明成功（具体到可打勾，如"上传后文档数=期望-跳过"）
关键决策：选 A 不选 B 的原因（一句话即可）
```

**为什么**：SDD 的核心教训——先写清楚"要什么"，代码才有对照标准，验收才不会靠感觉。上次"漫无目的上传 md"的教训：目的没写清，格式改了三版。

### 1.2 执行顺序（用 Claude Code 的 plan mode 制度化）

```
提出需求 → /plan 探索+计划 → 你批准计划 → 拆任务实施 → 对照验收标准逐项打勾
```

- 探索阶段先摸清现状再设计（这次 ragflow_upload 就是这么做的）
- 计划批准后不再临时改需求；有新想法记下来，下一轮再说
- **验收标准执行中证明不可行**（目标本身定错了）→ 暂停实施，回改规格并说明原因，不得悄悄放宽打勾标准

### 1.3 在制品（WIP）限制：1 + 1

**同一时间只做：1 个主力开发项目 + 1 个日常维护任务。**

- 主力项目 = 新功能/新项目（如 ragflow_upload）
- 维护 = 每日抓取、Cookie 更新这类例行公事
- 超过这个数就排队，不做第三个（上下文切换 15-30 分钟/次，比多线程更贵）

### 1.4 完成定义（Definition of Done）

一条工作只有满足以下才算"完成"：
1. 验收标准全部打勾
2. 冒烟测试跑通过（见 4.3）
3. 报告/日志能证明结果（如上传报告的统计表）
4. 踩过的坑已写入记忆库（feedback_*.md）

---

## 二、代码层：工程最小集

### 2.1 命名与风格（沿用现有习惯，PEP 8 为准）

| 对象 | 规则 | 示例 |
|---|---|---|
| 文件/模块 | 小写 + 下划线 | `ragflow_client.py` |
| 函数/变量 | snake_case | `load_cookies()` |
| 类 | CapWords | `RagflowClient` |
| 常量 | 全大写 | `MIN_TEXT_LEN = 20` |

- 4 空格缩进；导入分三组（标准库 → 第三方 → 本地），组间空行
- 打印日志沿用既有前缀约定：`[INFO] / [WARN] / [OK] / [SKIP] / [FAIL]`
- 运行统一 `C:/Python314/python.exe -X utf8`（GBK 控制台坑）

### 2.2 一条命令检查代码质量（Ruff）

```bash
C:/Python314/python.exe -m pip install ruff
ruff check .          # 静态检查（未用变量、未用 import、坏习惯）
ruff format .         # 自动格式化
ruff check --fix .    # 自动修复可修项（可选）
```

- 新写的文件跑一遍再提交；存量文件逐步过，不用一次清完
- 不追求零告警，但新代码不要引入新告警

### 2.3 项目结构模板

新项目（数据管线类）统一用这个骨架（ragflow_upload 已符合）：

```
项目名/
├── 项目名/            # 核心包：config.py / adapters.py / 业务模块
├── 入口脚本.py         # argparse 入口，只做参数解析和调度
├── config.yaml        # 配置（秘密只放 ${ENV_VAR} 引用）
├── requirements.txt
├── README.md          # 给「人」：怎么用、踩坑记录；CLAUDE.md 给「AI」：怎么跑、目录含义、项目特殊约定
├── tests/             # 冒烟测试（见 4.3）
├── state/  data/  logs/  reports/   # 运行产物，进 .gitignore
└── .gitignore
```

### 2.4 提交规范（沿用现有 Conventional Commits）

```
feat: 一句话主题
fix: 一句话主题
docs: 一句话主题
```

- 一次提交只做一件事；子仓库先提交，主仓库最后
- 提交信息写"为什么"比写"改了什么"更有价值
- 分支策略：默认直接推 master；只有跨多天的大改动才开 feature 分支

### 2.5 三条代码红线

1. **秘密一律环境变量**（`${RAGFLOW_API_KEY}`、`${LIANGKE_MYSQL_PASSWORD}`、`${DEEPSEEK_API_KEY_LOCALUSE}`），代码/配置里不得出现明文密码和 key；存放环境变量的 .env 类文件一律进 .gitignore，密钥文件不随仓库走（哪怕是私有仓库）
2. **禁止带日期的一次性脚本**（反例：`ingest_websearch_20260720.py` × 5 个变体）。正确做法：一个参数化脚本（如 `ingest_websearch.py --date 20260720`），过期的一次性脚本移入 `archive/`
3. **复制粘贴第三次出现时必须抽共享库**，抽到 `D:\Claude_code\lib\`，各项目改为 import。
   **存量过渡条款**：Config 类、DB 适配器、LLM 客户端目前在 rag_system / knowledge_graph / ragflow_upload 各有一份副本，属历史存量——下次任一项目改动到相关代码时顺手抽库，最迟 2026-10 完成，不要求一次性清理

---

## 三、数据层：单一事实源

### 3.1 权威归属表（谁生成、谁消费、修改入口唯一）

| 数据 | 权威源 | 消费者 | 备注 |
|---|---|---|---|
| 每日抓取+投融资 | MySQL liangke_scraper | 本地 RAG、看板、RAGFlow | 唯一写入方：scrape_daily.py + websearch 入库脚本 |
| 历史归档 | historical_final.db | 看板、RAGFlow | 只追加，不修改（改历史走人工审核流程） |
| 机构新闻 | institutions.db | 周报、知识图谱、RAGFlow | 唯一写入方：institution_news/run_all.py |
| 竞对档案 | profiles.db | 看板、RAGFlow | 手工+AI 共建，修改只走 profile 更新脚本 |
| 导出物（md/txt/Excel） | —（投影） | RAGFlow、下载 | **永远不手改**，改了源头后重跑生成 |

**为什么**：修改数据永远改源头数据库；md/Excel 只是投影。手改投影 = 源头和投影脱节 = 下次同步时被覆盖或污染对账。

### 3.2 标签统一格式

所有新打标一律用三分字典格式：

```json
{"weekly": ["资本运作"], "search_tags": ["国内投融资"], 
 "knowledge_graph": {"institutions": [], "technologies": ["超导"], "products": [], "people": []},
 "funding": {"round": "C轮", "amount": 300000000, "company": "量旋科技", "investors": [], "is_funding": true}}
```

- 旧 flat list 格式只在读取时兼容，不再新产生
- 读取方必须兼容两种格式（`isinstance(tags, list)` 判断）

### 3.3 数据库命名

沿用现有约定：量科每日库（MySQL liangke_scraper）vs 量科历史库（historical_final.db），新库命名先想清楚再建。

### 3.4 数据备份（数据是核心资产）

- 备份工具已有：`D:\Claude_code\backup.ps1`（一键打包全部 SQLite 库 + MySQL dump + 记忆库）
- **频率**：MySQL 每日库随每日抓取备份；历史库与三个 .db 每周至少一次；备份产物存 `archive/backups/`，并定期拷一份到网盘/异地
- **恢复演练**：每季度手动解压一份备份验证可恢复（没验证过的备份 = 没有备份）

---

## 四、AI 协作层：把 Claude Code 当团队用

### 4.1 三层上下文（从具体到持久）

| 层 | 载体 | 放什么 |
|---|---|---|
| 项目级 | 各仓库 CLAUDE.md / 项目 README | 该项目怎么跑、结构、约定 |
| 工作区级 | 本文件 + SOP.md + HANDOFF.md | 跨项目流程、每日例行、交接 |
| 持久记忆 | Claude 记忆库（memory/*.md） | 踩坑记录、用户偏好、架构决策 |

**新项目开工第一件事**：给该项目写一份极简 CLAUDE.md（怎么启动、依赖、目录含义），否则 AI 每次都要重新探索。

### 4.2 与 AI 协作的固定动作

- 超过半天/≥3 文件的功能：先规格 → plan mode → 批准 → 实施（见流程层）
- 改动完成后：跑冒烟测试 + 让 AI 对照验收标准自检
- 提交前：可用 `/code-review` 自审一次 diff
- 对话中踩到新坑：当场写进记忆库（feedback_*.md），下次自动生效
- 不确定的决策（范围/格式/取舍）：用提问让用户拍板，不猜

### 4.3 冒烟测试（最低限度测试，不是写测试框架）

每条关键管线配一个"跑通即绿"的测试脚本（tests/ 下，pytest 或纯脚本）。
**时机：改动到某管线相关代码后、提交前必跑一次该管线冒烟**（不是发布时才跑）：

| 管线 | 冒烟标准 |
|---|---|
| 量科网抓取 | 跑一遍 → 有新增/更新行 → cookie 未过期 |
| 打标 | 喂 3 条样例 → 输出含 weekly/funding 字段 |
| RAGFlow 上传 | dry-run → 导出文件数 = 期望 − 跳过 |
| 知识图谱 | 增量跑 → processed_state 游标推进 |

**为什么**：没有测试 = 每次重构是赌博 = 不敢重构 = 屎山成型。冒烟测试成本是一小时，收益是"以后敢改"。

---

## 五、防屎山三条总红线（贴在显眼处）

1. **不做第四个副本** —— 见 2.5 红线 3（含存量三副本的过渡安排）
2. **不建带日期的一次性脚本** —— 参数化；用完归档
3. **不绕开事实源改数据** —— 改数据库，然后重跑生成；投影文件永不手改

> 规则的解释权：每条规则的"为什么"都写在对应小节。遇到规则不适用的情况，
> 改规则本身（并说明原因），而不是悄悄绕过。

---

## 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-09-01 | 初版 | 屎山防控 + 全网调研（敏捷/PEP 8/solo 实践/SDD）落地 |
| 2026-09-01 | 合入外部评审意见：存量副本过渡期（最迟 2026-10 抽库）、数据备份频率与恢复演练、规格门槛统一（半天 或 ≥3 文件）、验收不可行时的回改流程、.env 不进仓库、README/CLAUDE.md 分工、冒烟测试时机、Ruff --fix、git 分支策略 | 另一模型评审，逐条复核通过 |
